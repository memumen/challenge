export const fecthPath = () => `${window.location.origin}/files`;
export const deletePath = (id = ':id') => `${window.location.origin}/files/${id}`;
export const attachmentCreatePath = () => `${window.location.origin}/files/attachment_create`;
